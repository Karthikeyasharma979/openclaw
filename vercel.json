{
  "version": 2,
  "builds": [
    { "src": "index.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "index.js" }
  ],
  "buildCommand": "pnpm install --ignore-scripts",
  "installCommand": "pnpm install --ignore-scripts"
}
